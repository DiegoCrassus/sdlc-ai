# INVESTIMENTS-4 — Frontend Shell

**GitHub:** #28 | **Branch:** `feature/INVESTIMENTS-4-issue-gh-28-frontend-shell`

## Acceptance Criteria

1. `npm run build` in `app/frontend` exits 0.
2. App renders layout with nav: Dashboard, Search, Portfolio, Watchlist, Alerts, Sources.
3. Vite proxies `/api` to backend port 8000.
4. `npm run lint` exits 0 (if configured).
