---
description: React + Vite frontend (provisional UI)
globs: apps/web/**/*
alwaysApply: false
---

# Frontend React

- Vite proxy: `/v1` → `localhost:8000`
- Pages: Home, CreateCampaign, Workspace
- `SheetCanvas` — renderiza `canvas_spec` + mock data + template image

## API client

`apps/web/src/api.ts` — tipos alinhados ao backend

## Estilo

- CSS simples em `index.css` + colocated component CSS
- UI provisória — priorizar fluxo GM/jogador sobre polish

## Futuro

Importar tipos de `generated/typescript/` após `rpg compile`
